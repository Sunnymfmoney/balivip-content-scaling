# Composition assets

Drop the footage and photographs this composition uses here.

Not tracked in git: these are BaliVIP's own wedding files and they are large. Pull
them from the shared Drive.

`index.html` currently expects `hook.jpg`, `teach.jpg` and `cta.jpg`. Swap in
whatever the post actually needs and update the `src` attributes.
